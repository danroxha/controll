package com.start.controll.repositories;

import com.start.controll.entities.RegisterDaily;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegisterDailyRepository extends JpaRepository<RegisterDaily, Long> {
}
